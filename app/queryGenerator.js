

var requestBody = [];
var queryGenerator = function (userInfo, report) {
    if (userInfo)

    //From foreign key
    requestBody.push({viewId: '129070637'});
    var metrics = [];
    foreach()

}